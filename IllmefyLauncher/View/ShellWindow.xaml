<Window x:Class="IllmefyLauncher.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:IllmefyLauncher.ViewModel"
        mc:Ignorable="d"
        Title="ShellWindow" Height="450" Width="800">
    <Window.Resources>
        <viewModel:ShellWindowViewModel x:Key="ViewModel"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource ViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Image Grid.RowSpan="2" Stretch="UniformToFill" Source="{Binding Background}"/>
        <ListView Margin="10,0,10,0" Background="Transparent" SelectedItem="{Binding SelectedView}" BorderThickness="0" BorderBrush="Transparent" ItemContainerStyle="{DynamicResource TopMenuListViewItemContainerStyle}"  ItemsSource="{Binding MenuItems}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        <Grid Grid.Row="1">
            <ContentControl Content="{Binding SelectedViewModel}"/>
        </Grid>
    </Grid>
</Window>
